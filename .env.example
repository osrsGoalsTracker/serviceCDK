# AWS Account Configuration
AWS_ACCOUNT=084375548651
AWS_REGION=us-west-2
STAGE=dev

# Credential Type (SSO or LOCAL)
USE_SSO=true

# SSO Configuration (used if USE_SSO=true)
SSO_PROFILE=AdministratorAccess-084375548651

# Local Profile Configuration (used if USE_SSO=false)
LOCAL_PROFILE=default 